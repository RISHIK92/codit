// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: resourceProgress.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "resourceProgress";

export interface GetPhaseResourcesRequest {
  phaseId: string;
  userEmail: string;
  projectId: string;
}

export interface ResourceItem {
  id: string;
  phaseId: string;
  /** "video" | "article" | "docs" | etc. */
  type: string;
  title: string;
  url: string;
  durationMinutes: number;
  provider: string;
  qualityScore: number;
  /** per-user completion, injected server-side */
  completed: boolean;
  /** ISO string, empty if not completed */
  completedAt: string;
}

export interface GetPhaseResourcesResponse {
  resources: ResourceItem[];
}

export interface MarkCompletedRequest {
  resourceId: string;
  userEmail: string;
  projectId: string;
  completed: boolean;
}

export interface MarkCompletedResponse {
  success: boolean;
}

export interface GetProgressRequest {
  userEmail: string;
  projectId: string;
}

export interface ProgressEntry {
  resourceId: string;
  completed: boolean;
  /** ISO string, empty if not completed */
  completedAt: string;
}

export interface GetProgressResponse {
  entries: ProgressEntry[];
}

function createBaseGetPhaseResourcesRequest(): GetPhaseResourcesRequest {
  return { phaseId: "", userEmail: "", projectId: "" };
}

export const GetPhaseResourcesRequest: MessageFns<GetPhaseResourcesRequest> = {
  encode(
    message: GetPhaseResourcesRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.phaseId !== "") {
      writer.uint32(10).string(message.phaseId);
    }
    if (message.userEmail !== "") {
      writer.uint32(18).string(message.userEmail);
    }
    if (message.projectId !== "") {
      writer.uint32(26).string(message.projectId);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): GetPhaseResourcesRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPhaseResourcesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.phaseId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userEmail = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPhaseResourcesRequest {
    return {
      phaseId: isSet(object.phaseId)
        ? globalThis.String(object.phaseId)
        : isSet(object.phase_id)
          ? globalThis.String(object.phase_id)
          : "",
      userEmail: isSet(object.userEmail)
        ? globalThis.String(object.userEmail)
        : isSet(object.user_email)
          ? globalThis.String(object.user_email)
          : "",
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
          ? globalThis.String(object.project_id)
          : "",
    };
  },

  toJSON(message: GetPhaseResourcesRequest): unknown {
    const obj: any = {};
    if (message.phaseId !== "") {
      obj.phaseId = message.phaseId;
    }
    if (message.userEmail !== "") {
      obj.userEmail = message.userEmail;
    }
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPhaseResourcesRequest>, I>>(
    base?: I,
  ): GetPhaseResourcesRequest {
    return GetPhaseResourcesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPhaseResourcesRequest>, I>>(
    object: I,
  ): GetPhaseResourcesRequest {
    const message = createBaseGetPhaseResourcesRequest();
    message.phaseId = object.phaseId ?? "";
    message.userEmail = object.userEmail ?? "";
    message.projectId = object.projectId ?? "";
    return message;
  },
};

function createBaseResourceItem(): ResourceItem {
  return {
    id: "",
    phaseId: "",
    type: "",
    title: "",
    url: "",
    durationMinutes: 0,
    provider: "",
    qualityScore: 0,
    completed: false,
    completedAt: "",
  };
}

export const ResourceItem: MessageFns<ResourceItem> = {
  encode(
    message: ResourceItem,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.phaseId !== "") {
      writer.uint32(18).string(message.phaseId);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.url !== "") {
      writer.uint32(42).string(message.url);
    }
    if (message.durationMinutes !== 0) {
      writer.uint32(48).int32(message.durationMinutes);
    }
    if (message.provider !== "") {
      writer.uint32(58).string(message.provider);
    }
    if (message.qualityScore !== 0) {
      writer.uint32(69).float(message.qualityScore);
    }
    if (message.completed !== false) {
      writer.uint32(72).bool(message.completed);
    }
    if (message.completedAt !== "") {
      writer.uint32(82).string(message.completedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceItem {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.phaseId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.durationMinutes = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.qualityScore = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.completed = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.completedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceItem {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      phaseId: isSet(object.phaseId)
        ? globalThis.String(object.phaseId)
        : isSet(object.phase_id)
          ? globalThis.String(object.phase_id)
          : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      durationMinutes: isSet(object.durationMinutes)
        ? globalThis.Number(object.durationMinutes)
        : isSet(object.duration_minutes)
          ? globalThis.Number(object.duration_minutes)
          : 0,
      provider: isSet(object.provider)
        ? globalThis.String(object.provider)
        : "",
      qualityScore: isSet(object.qualityScore)
        ? globalThis.Number(object.qualityScore)
        : isSet(object.quality_score)
          ? globalThis.Number(object.quality_score)
          : 0,
      completed: isSet(object.completed)
        ? globalThis.Boolean(object.completed)
        : false,
      completedAt: isSet(object.completedAt)
        ? globalThis.String(object.completedAt)
        : isSet(object.completed_at)
          ? globalThis.String(object.completed_at)
          : "",
    };
  },

  toJSON(message: ResourceItem): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.phaseId !== "") {
      obj.phaseId = message.phaseId;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.durationMinutes !== 0) {
      obj.durationMinutes = Math.round(message.durationMinutes);
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.qualityScore !== 0) {
      obj.qualityScore = message.qualityScore;
    }
    if (message.completed !== false) {
      obj.completed = message.completed;
    }
    if (message.completedAt !== "") {
      obj.completedAt = message.completedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResourceItem>, I>>(
    base?: I,
  ): ResourceItem {
    return ResourceItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceItem>, I>>(
    object: I,
  ): ResourceItem {
    const message = createBaseResourceItem();
    message.id = object.id ?? "";
    message.phaseId = object.phaseId ?? "";
    message.type = object.type ?? "";
    message.title = object.title ?? "";
    message.url = object.url ?? "";
    message.durationMinutes = object.durationMinutes ?? 0;
    message.provider = object.provider ?? "";
    message.qualityScore = object.qualityScore ?? 0;
    message.completed = object.completed ?? false;
    message.completedAt = object.completedAt ?? "";
    return message;
  },
};

function createBaseGetPhaseResourcesResponse(): GetPhaseResourcesResponse {
  return { resources: [] };
}

export const GetPhaseResourcesResponse: MessageFns<GetPhaseResourcesResponse> =
  {
    encode(
      message: GetPhaseResourcesResponse,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      for (const v of message.resources) {
        ResourceItem.encode(v!, writer.uint32(10).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): GetPhaseResourcesResponse {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetPhaseResourcesResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.resources.push(
              ResourceItem.decode(reader, reader.uint32()),
            );
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): GetPhaseResourcesResponse {
      return {
        resources: globalThis.Array.isArray(object?.resources)
          ? object.resources.map((e: any) => ResourceItem.fromJSON(e))
          : [],
      };
    },

    toJSON(message: GetPhaseResourcesResponse): unknown {
      const obj: any = {};
      if (message.resources?.length) {
        obj.resources = message.resources.map((e) => ResourceItem.toJSON(e));
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<GetPhaseResourcesResponse>, I>>(
      base?: I,
    ): GetPhaseResourcesResponse {
      return GetPhaseResourcesResponse.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<GetPhaseResourcesResponse>, I>>(
      object: I,
    ): GetPhaseResourcesResponse {
      const message = createBaseGetPhaseResourcesResponse();
      message.resources =
        object.resources?.map((e) => ResourceItem.fromPartial(e)) || [];
      return message;
    },
  };

function createBaseMarkCompletedRequest(): MarkCompletedRequest {
  return { resourceId: "", userEmail: "", projectId: "", completed: false };
}

export const MarkCompletedRequest: MessageFns<MarkCompletedRequest> = {
  encode(
    message: MarkCompletedRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.resourceId !== "") {
      writer.uint32(10).string(message.resourceId);
    }
    if (message.userEmail !== "") {
      writer.uint32(18).string(message.userEmail);
    }
    if (message.projectId !== "") {
      writer.uint32(26).string(message.projectId);
    }
    if (message.completed !== false) {
      writer.uint32(32).bool(message.completed);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): MarkCompletedRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkCompletedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resourceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userEmail = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.completed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkCompletedRequest {
    return {
      resourceId: isSet(object.resourceId)
        ? globalThis.String(object.resourceId)
        : isSet(object.resource_id)
          ? globalThis.String(object.resource_id)
          : "",
      userEmail: isSet(object.userEmail)
        ? globalThis.String(object.userEmail)
        : isSet(object.user_email)
          ? globalThis.String(object.user_email)
          : "",
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
          ? globalThis.String(object.project_id)
          : "",
      completed: isSet(object.completed)
        ? globalThis.Boolean(object.completed)
        : false,
    };
  },

  toJSON(message: MarkCompletedRequest): unknown {
    const obj: any = {};
    if (message.resourceId !== "") {
      obj.resourceId = message.resourceId;
    }
    if (message.userEmail !== "") {
      obj.userEmail = message.userEmail;
    }
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.completed !== false) {
      obj.completed = message.completed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarkCompletedRequest>, I>>(
    base?: I,
  ): MarkCompletedRequest {
    return MarkCompletedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarkCompletedRequest>, I>>(
    object: I,
  ): MarkCompletedRequest {
    const message = createBaseMarkCompletedRequest();
    message.resourceId = object.resourceId ?? "";
    message.userEmail = object.userEmail ?? "";
    message.projectId = object.projectId ?? "";
    message.completed = object.completed ?? false;
    return message;
  },
};

function createBaseMarkCompletedResponse(): MarkCompletedResponse {
  return { success: false };
}

export const MarkCompletedResponse: MessageFns<MarkCompletedResponse> = {
  encode(
    message: MarkCompletedResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): MarkCompletedResponse {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkCompletedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkCompletedResponse {
    return {
      success: isSet(object.success)
        ? globalThis.Boolean(object.success)
        : false,
    };
  },

  toJSON(message: MarkCompletedResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarkCompletedResponse>, I>>(
    base?: I,
  ): MarkCompletedResponse {
    return MarkCompletedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarkCompletedResponse>, I>>(
    object: I,
  ): MarkCompletedResponse {
    const message = createBaseMarkCompletedResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseGetProgressRequest(): GetProgressRequest {
  return { userEmail: "", projectId: "" };
}

export const GetProgressRequest: MessageFns<GetProgressRequest> = {
  encode(
    message: GetProgressRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.userEmail !== "") {
      writer.uint32(10).string(message.userEmail);
    }
    if (message.projectId !== "") {
      writer.uint32(18).string(message.projectId);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): GetProgressRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProgressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userEmail = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProgressRequest {
    return {
      userEmail: isSet(object.userEmail)
        ? globalThis.String(object.userEmail)
        : isSet(object.user_email)
          ? globalThis.String(object.user_email)
          : "",
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
          ? globalThis.String(object.project_id)
          : "",
    };
  },

  toJSON(message: GetProgressRequest): unknown {
    const obj: any = {};
    if (message.userEmail !== "") {
      obj.userEmail = message.userEmail;
    }
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProgressRequest>, I>>(
    base?: I,
  ): GetProgressRequest {
    return GetProgressRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProgressRequest>, I>>(
    object: I,
  ): GetProgressRequest {
    const message = createBaseGetProgressRequest();
    message.userEmail = object.userEmail ?? "";
    message.projectId = object.projectId ?? "";
    return message;
  },
};

function createBaseProgressEntry(): ProgressEntry {
  return { resourceId: "", completed: false, completedAt: "" };
}

export const ProgressEntry: MessageFns<ProgressEntry> = {
  encode(
    message: ProgressEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.resourceId !== "") {
      writer.uint32(10).string(message.resourceId);
    }
    if (message.completed !== false) {
      writer.uint32(16).bool(message.completed);
    }
    if (message.completedAt !== "") {
      writer.uint32(26).string(message.completedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProgressEntry {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProgressEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resourceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.completed = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.completedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProgressEntry {
    return {
      resourceId: isSet(object.resourceId)
        ? globalThis.String(object.resourceId)
        : isSet(object.resource_id)
          ? globalThis.String(object.resource_id)
          : "",
      completed: isSet(object.completed)
        ? globalThis.Boolean(object.completed)
        : false,
      completedAt: isSet(object.completedAt)
        ? globalThis.String(object.completedAt)
        : isSet(object.completed_at)
          ? globalThis.String(object.completed_at)
          : "",
    };
  },

  toJSON(message: ProgressEntry): unknown {
    const obj: any = {};
    if (message.resourceId !== "") {
      obj.resourceId = message.resourceId;
    }
    if (message.completed !== false) {
      obj.completed = message.completed;
    }
    if (message.completedAt !== "") {
      obj.completedAt = message.completedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProgressEntry>, I>>(
    base?: I,
  ): ProgressEntry {
    return ProgressEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProgressEntry>, I>>(
    object: I,
  ): ProgressEntry {
    const message = createBaseProgressEntry();
    message.resourceId = object.resourceId ?? "";
    message.completed = object.completed ?? false;
    message.completedAt = object.completedAt ?? "";
    return message;
  },
};

function createBaseGetProgressResponse(): GetProgressResponse {
  return { entries: [] };
}

export const GetProgressResponse: MessageFns<GetProgressResponse> = {
  encode(
    message: GetProgressResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.entries) {
      ProgressEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): GetProgressResponse {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProgressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(ProgressEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProgressResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => ProgressEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetProgressResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => ProgressEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProgressResponse>, I>>(
    base?: I,
  ): GetProgressResponse {
    return GetProgressResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProgressResponse>, I>>(
    object: I,
  ): GetProgressResponse {
    const message = createBaseGetProgressResponse();
    message.entries =
      object.entries?.map((e) => ProgressEntry.fromPartial(e)) || [];
    return message;
  },
};

export type ResourceProgressServiceService =
  typeof ResourceProgressServiceService;
export const ResourceProgressServiceService = {
  /** Fetch all resources for a phase, with per-user completion status */
  getPhaseResources: {
    path: "/resourceProgress.ResourceProgressService/GetPhaseResources" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetPhaseResourcesRequest): Buffer =>
      Buffer.from(GetPhaseResourcesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetPhaseResourcesRequest =>
      GetPhaseResourcesRequest.decode(value),
    responseSerialize: (value: GetPhaseResourcesResponse): Buffer =>
      Buffer.from(GetPhaseResourcesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetPhaseResourcesResponse =>
      GetPhaseResourcesResponse.decode(value),
  },
  /** Mark or unmark a single resource as completed */
  markCompleted: {
    path: "/resourceProgress.ResourceProgressService/MarkCompleted" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: MarkCompletedRequest): Buffer =>
      Buffer.from(MarkCompletedRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): MarkCompletedRequest =>
      MarkCompletedRequest.decode(value),
    responseSerialize: (value: MarkCompletedResponse): Buffer =>
      Buffer.from(MarkCompletedResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): MarkCompletedResponse =>
      MarkCompletedResponse.decode(value),
  },
  /** Fetch all completion records for a user+project (lightweight, all phases) */
  getProgress: {
    path: "/resourceProgress.ResourceProgressService/GetProgress" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetProgressRequest): Buffer =>
      Buffer.from(GetProgressRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetProgressRequest =>
      GetProgressRequest.decode(value),
    responseSerialize: (value: GetProgressResponse): Buffer =>
      Buffer.from(GetProgressResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetProgressResponse =>
      GetProgressResponse.decode(value),
  },
} as const;

export interface ResourceProgressServiceServer extends UntypedServiceImplementation {
  /** Fetch all resources for a phase, with per-user completion status */
  getPhaseResources: handleUnaryCall<
    GetPhaseResourcesRequest,
    GetPhaseResourcesResponse
  >;
  /** Mark or unmark a single resource as completed */
  markCompleted: handleUnaryCall<MarkCompletedRequest, MarkCompletedResponse>;
  /** Fetch all completion records for a user+project (lightweight, all phases) */
  getProgress: handleUnaryCall<GetProgressRequest, GetProgressResponse>;
}

export interface ResourceProgressServiceClient extends Client {
  /** Fetch all resources for a phase, with per-user completion status */
  getPhaseResources(
    request: GetPhaseResourcesRequest,
    callback: (
      error: ServiceError | null,
      response: GetPhaseResourcesResponse,
    ) => void,
  ): ClientUnaryCall;
  getPhaseResources(
    request: GetPhaseResourcesRequest,
    metadata: Metadata,
    callback: (
      error: ServiceError | null,
      response: GetPhaseResourcesResponse,
    ) => void,
  ): ClientUnaryCall;
  getPhaseResources(
    request: GetPhaseResourcesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (
      error: ServiceError | null,
      response: GetPhaseResourcesResponse,
    ) => void,
  ): ClientUnaryCall;
  /** Mark or unmark a single resource as completed */
  markCompleted(
    request: MarkCompletedRequest,
    callback: (
      error: ServiceError | null,
      response: MarkCompletedResponse,
    ) => void,
  ): ClientUnaryCall;
  markCompleted(
    request: MarkCompletedRequest,
    metadata: Metadata,
    callback: (
      error: ServiceError | null,
      response: MarkCompletedResponse,
    ) => void,
  ): ClientUnaryCall;
  markCompleted(
    request: MarkCompletedRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (
      error: ServiceError | null,
      response: MarkCompletedResponse,
    ) => void,
  ): ClientUnaryCall;
  /** Fetch all completion records for a user+project (lightweight, all phases) */
  getProgress(
    request: GetProgressRequest,
    callback: (
      error: ServiceError | null,
      response: GetProgressResponse,
    ) => void,
  ): ClientUnaryCall;
  getProgress(
    request: GetProgressRequest,
    metadata: Metadata,
    callback: (
      error: ServiceError | null,
      response: GetProgressResponse,
    ) => void,
  ): ClientUnaryCall;
  getProgress(
    request: GetProgressRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (
      error: ServiceError | null,
      response: GetProgressResponse,
    ) => void,
  ): ClientUnaryCall;
}

export const ResourceProgressServiceClient = makeGenericClientConstructor(
  ResourceProgressServiceService,
  "resourceProgress.ResourceProgressService",
) as unknown as {
  new (
    address: string,
    credentials: ChannelCredentials,
    options?: Partial<ClientOptions>,
  ): ResourceProgressServiceClient;
  service: typeof ResourceProgressServiceService;
  serviceName: string;
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
