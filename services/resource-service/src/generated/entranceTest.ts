// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: entranceTest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "entrancetest";

export interface StartTestRequest {
  email: string;
}

export interface StartTestResponse {
  /** first 2 easy questions */
  questions: EntranceQuestionDTO[];
  attemptId: string;
  round: number;
}

export interface SubmitRoundRequest {
  email: string;
  answers: AnswerDTO[];
}

export interface SubmitRoundResponse {
  done: boolean;
  /** populated when done = true */
  skillLevel: string;
  /** populated when done = false */
  nextQuestions: EntranceQuestionDTO[];
  round: number;
}

export interface EntranceQuestionDTO {
  id: string;
  question: string;
  options: string[];
  difficulty: string;
  /** correct_option intentionally omitted — never sent to client */
  topic: string;
}

export interface AnswerDTO {
  questionId: string;
  selectedOption: number;
}

function createBaseStartTestRequest(): StartTestRequest {
  return { email: "" };
}

export const StartTestRequest: MessageFns<StartTestRequest> = {
  encode(message: StartTestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartTestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartTestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartTestRequest {
    return { email: isSet(object.email) ? globalThis.String(object.email) : "" };
  },

  toJSON(message: StartTestRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartTestRequest>, I>>(base?: I): StartTestRequest {
    return StartTestRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartTestRequest>, I>>(object: I): StartTestRequest {
    const message = createBaseStartTestRequest();
    message.email = object.email ?? "";
    return message;
  },
};

function createBaseStartTestResponse(): StartTestResponse {
  return { questions: [], attemptId: "", round: 0 };
}

export const StartTestResponse: MessageFns<StartTestResponse> = {
  encode(message: StartTestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.questions) {
      EntranceQuestionDTO.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.attemptId !== "") {
      writer.uint32(18).string(message.attemptId);
    }
    if (message.round !== 0) {
      writer.uint32(24).int32(message.round);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartTestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartTestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.questions.push(EntranceQuestionDTO.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.attemptId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.round = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartTestResponse {
    return {
      questions: globalThis.Array.isArray(object?.questions)
        ? object.questions.map((e: any) => EntranceQuestionDTO.fromJSON(e))
        : [],
      attemptId: isSet(object.attemptId)
        ? globalThis.String(object.attemptId)
        : isSet(object.attempt_id)
        ? globalThis.String(object.attempt_id)
        : "",
      round: isSet(object.round) ? globalThis.Number(object.round) : 0,
    };
  },

  toJSON(message: StartTestResponse): unknown {
    const obj: any = {};
    if (message.questions?.length) {
      obj.questions = message.questions.map((e) => EntranceQuestionDTO.toJSON(e));
    }
    if (message.attemptId !== "") {
      obj.attemptId = message.attemptId;
    }
    if (message.round !== 0) {
      obj.round = Math.round(message.round);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartTestResponse>, I>>(base?: I): StartTestResponse {
    return StartTestResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartTestResponse>, I>>(object: I): StartTestResponse {
    const message = createBaseStartTestResponse();
    message.questions = object.questions?.map((e) => EntranceQuestionDTO.fromPartial(e)) || [];
    message.attemptId = object.attemptId ?? "";
    message.round = object.round ?? 0;
    return message;
  },
};

function createBaseSubmitRoundRequest(): SubmitRoundRequest {
  return { email: "", answers: [] };
}

export const SubmitRoundRequest: MessageFns<SubmitRoundRequest> = {
  encode(message: SubmitRoundRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    for (const v of message.answers) {
      AnswerDTO.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitRoundRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitRoundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.answers.push(AnswerDTO.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitRoundRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      answers: globalThis.Array.isArray(object?.answers) ? object.answers.map((e: any) => AnswerDTO.fromJSON(e)) : [],
    };
  },

  toJSON(message: SubmitRoundRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.answers?.length) {
      obj.answers = message.answers.map((e) => AnswerDTO.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitRoundRequest>, I>>(base?: I): SubmitRoundRequest {
    return SubmitRoundRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitRoundRequest>, I>>(object: I): SubmitRoundRequest {
    const message = createBaseSubmitRoundRequest();
    message.email = object.email ?? "";
    message.answers = object.answers?.map((e) => AnswerDTO.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSubmitRoundResponse(): SubmitRoundResponse {
  return { done: false, skillLevel: "", nextQuestions: [], round: 0 };
}

export const SubmitRoundResponse: MessageFns<SubmitRoundResponse> = {
  encode(message: SubmitRoundResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.done !== false) {
      writer.uint32(8).bool(message.done);
    }
    if (message.skillLevel !== "") {
      writer.uint32(18).string(message.skillLevel);
    }
    for (const v of message.nextQuestions) {
      EntranceQuestionDTO.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.round !== 0) {
      writer.uint32(32).int32(message.round);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitRoundResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitRoundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.done = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.skillLevel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nextQuestions.push(EntranceQuestionDTO.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.round = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitRoundResponse {
    return {
      done: isSet(object.done) ? globalThis.Boolean(object.done) : false,
      skillLevel: isSet(object.skillLevel)
        ? globalThis.String(object.skillLevel)
        : isSet(object.skill_level)
        ? globalThis.String(object.skill_level)
        : "",
      nextQuestions: globalThis.Array.isArray(object?.nextQuestions)
        ? object.nextQuestions.map((e: any) => EntranceQuestionDTO.fromJSON(e))
        : globalThis.Array.isArray(object?.next_questions)
        ? object.next_questions.map((e: any) => EntranceQuestionDTO.fromJSON(e))
        : [],
      round: isSet(object.round) ? globalThis.Number(object.round) : 0,
    };
  },

  toJSON(message: SubmitRoundResponse): unknown {
    const obj: any = {};
    if (message.done !== false) {
      obj.done = message.done;
    }
    if (message.skillLevel !== "") {
      obj.skillLevel = message.skillLevel;
    }
    if (message.nextQuestions?.length) {
      obj.nextQuestions = message.nextQuestions.map((e) => EntranceQuestionDTO.toJSON(e));
    }
    if (message.round !== 0) {
      obj.round = Math.round(message.round);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitRoundResponse>, I>>(base?: I): SubmitRoundResponse {
    return SubmitRoundResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitRoundResponse>, I>>(object: I): SubmitRoundResponse {
    const message = createBaseSubmitRoundResponse();
    message.done = object.done ?? false;
    message.skillLevel = object.skillLevel ?? "";
    message.nextQuestions = object.nextQuestions?.map((e) => EntranceQuestionDTO.fromPartial(e)) || [];
    message.round = object.round ?? 0;
    return message;
  },
};

function createBaseEntranceQuestionDTO(): EntranceQuestionDTO {
  return { id: "", question: "", options: [], difficulty: "", topic: "" };
}

export const EntranceQuestionDTO: MessageFns<EntranceQuestionDTO> = {
  encode(message: EntranceQuestionDTO, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.question !== "") {
      writer.uint32(18).string(message.question);
    }
    for (const v of message.options) {
      writer.uint32(26).string(v!);
    }
    if (message.difficulty !== "") {
      writer.uint32(34).string(message.difficulty);
    }
    if (message.topic !== "") {
      writer.uint32(42).string(message.topic);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EntranceQuestionDTO {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEntranceQuestionDTO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.question = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.options.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.difficulty = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.topic = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EntranceQuestionDTO {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      question: isSet(object.question) ? globalThis.String(object.question) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e: any) => globalThis.String(e)) : [],
      difficulty: isSet(object.difficulty) ? globalThis.String(object.difficulty) : "",
      topic: isSet(object.topic) ? globalThis.String(object.topic) : "",
    };
  },

  toJSON(message: EntranceQuestionDTO): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.question !== "") {
      obj.question = message.question;
    }
    if (message.options?.length) {
      obj.options = message.options;
    }
    if (message.difficulty !== "") {
      obj.difficulty = message.difficulty;
    }
    if (message.topic !== "") {
      obj.topic = message.topic;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EntranceQuestionDTO>, I>>(base?: I): EntranceQuestionDTO {
    return EntranceQuestionDTO.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EntranceQuestionDTO>, I>>(object: I): EntranceQuestionDTO {
    const message = createBaseEntranceQuestionDTO();
    message.id = object.id ?? "";
    message.question = object.question ?? "";
    message.options = object.options?.map((e) => e) || [];
    message.difficulty = object.difficulty ?? "";
    message.topic = object.topic ?? "";
    return message;
  },
};

function createBaseAnswerDTO(): AnswerDTO {
  return { questionId: "", selectedOption: 0 };
}

export const AnswerDTO: MessageFns<AnswerDTO> = {
  encode(message: AnswerDTO, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.questionId !== "") {
      writer.uint32(10).string(message.questionId);
    }
    if (message.selectedOption !== 0) {
      writer.uint32(16).int32(message.selectedOption);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnswerDTO {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnswerDTO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.questionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.selectedOption = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnswerDTO {
    return {
      questionId: isSet(object.questionId)
        ? globalThis.String(object.questionId)
        : isSet(object.question_id)
        ? globalThis.String(object.question_id)
        : "",
      selectedOption: isSet(object.selectedOption)
        ? globalThis.Number(object.selectedOption)
        : isSet(object.selected_option)
        ? globalThis.Number(object.selected_option)
        : 0,
    };
  },

  toJSON(message: AnswerDTO): unknown {
    const obj: any = {};
    if (message.questionId !== "") {
      obj.questionId = message.questionId;
    }
    if (message.selectedOption !== 0) {
      obj.selectedOption = Math.round(message.selectedOption);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnswerDTO>, I>>(base?: I): AnswerDTO {
    return AnswerDTO.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnswerDTO>, I>>(object: I): AnswerDTO {
    const message = createBaseAnswerDTO();
    message.questionId = object.questionId ?? "";
    message.selectedOption = object.selectedOption ?? 0;
    return message;
  },
};

export type EntranceTestServiceService = typeof EntranceTestServiceService;
export const EntranceTestServiceService = {
  startTest: {
    path: "/entrancetest.EntranceTestService/StartTest" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: StartTestRequest): Buffer => Buffer.from(StartTestRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StartTestRequest => StartTestRequest.decode(value),
    responseSerialize: (value: StartTestResponse): Buffer => Buffer.from(StartTestResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): StartTestResponse => StartTestResponse.decode(value),
  },
  submitRound: {
    path: "/entrancetest.EntranceTestService/SubmitRound" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SubmitRoundRequest): Buffer => Buffer.from(SubmitRoundRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SubmitRoundRequest => SubmitRoundRequest.decode(value),
    responseSerialize: (value: SubmitRoundResponse): Buffer => Buffer.from(SubmitRoundResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SubmitRoundResponse => SubmitRoundResponse.decode(value),
  },
} as const;

export interface EntranceTestServiceServer extends UntypedServiceImplementation {
  startTest: handleUnaryCall<StartTestRequest, StartTestResponse>;
  submitRound: handleUnaryCall<SubmitRoundRequest, SubmitRoundResponse>;
}

export interface EntranceTestServiceClient extends Client {
  startTest(
    request: StartTestRequest,
    callback: (error: ServiceError | null, response: StartTestResponse) => void,
  ): ClientUnaryCall;
  startTest(
    request: StartTestRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: StartTestResponse) => void,
  ): ClientUnaryCall;
  startTest(
    request: StartTestRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StartTestResponse) => void,
  ): ClientUnaryCall;
  submitRound(
    request: SubmitRoundRequest,
    callback: (error: ServiceError | null, response: SubmitRoundResponse) => void,
  ): ClientUnaryCall;
  submitRound(
    request: SubmitRoundRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SubmitRoundResponse) => void,
  ): ClientUnaryCall;
  submitRound(
    request: SubmitRoundRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SubmitRoundResponse) => void,
  ): ClientUnaryCall;
}

export const EntranceTestServiceClient = makeGenericClientConstructor(
  EntranceTestServiceService,
  "entrancetest.EntranceTestService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): EntranceTestServiceClient;
  service: typeof EntranceTestServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
